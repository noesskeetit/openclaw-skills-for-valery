use_default_settings: true

search:
  formats:
    - html
    - json

server:
  limiter: true
  image_proxy: false
  secret_key: "openclaw-secret"
  port: 8080
  bind_address: "0.0.0.0"
  method: "GET"

general:
  enable_metrics: false
