playwright>=1.40
beautifulsoup4>=4.12
markdownify>=0.11
# lxml is optional but recommended: BeautifulSoup uses it as the parser in
# extract_content.py. Without it, install html.parser fallback manually.
lxml>=4.9
